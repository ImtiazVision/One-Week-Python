# for loop has a variable and we can name the variable anything we want. It also has an iterable(i.e. strings, lists, tuples) and finally it has a statement that runs. 

word = "Hello, World!"

for var in word:
  print(var)