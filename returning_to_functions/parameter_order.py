# When defining functions, the order of parameters matters! The default argument has to come second after regular parameters.

# this is the order which parameter follows
# (parameters, *args, default parameters, **kwargs)